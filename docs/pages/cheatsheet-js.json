{"id": "cheatsheet-js", "title": "Шпаргалка JS/TS", "fullTitle": "JavaScript &amp; TypeScript — шпаргалка", "html": "<h1>JavaScript &amp; TypeScript — шпаргалка</h1>\n<blockquote>\n<p>После недель 5–10.</p>\n</blockquote>\n<h2>Типы и сравнение</h2>\n<ul>\n<li>Примитивы: <code>string</code>, <code>number</code>, <code>boolean</code>, <code>null</code>, <code>undefined</code>, <code>bigint</code>, <code>symbol</code></li>\n<li>Всегда <code>===</code>, не <code>==</code></li>\n<li><code>const</code> по умолчанию; <code>let</code> если переприсвоение</li>\n</ul>\n<h2>Массивы</h2>\n<pre><code class=\"language-js\">arr.map(x =&gt; x * 2)\narr.filter(x =&gt; x.done)\narr.find(x =&gt; x.id === id)\narr.reduce((sum, x) =&gt; sum + x, 0)\n// slice — копия; splice — мутирует\n</code></pre>\n<h2>Async</h2>\n<pre><code class=\"language-js\">async function load() {\n  const res = await fetch(url);\n  if (!res.ok) throw new Error(res.status);\n  return res.json();\n}\n</code></pre>\n<h2>Event Loop (упрощённо)</h2>\n<pre><code>sync code → microtasks (Promise) → macrotasks (setTimeout) → render\n</code></pre>\n<h2>Модули</h2>\n<pre><code class=\"language-js\">// utils.js\nexport function fmt(d) { ... }\n// app.js\nimport { fmt } from './utils.js';\n</code></pre>\n<h2>TypeScript</h2>\n<pre><code class=\"language-ts\">interface User { id: number; name: string }\ntype Status = 'active' | 'done';\nfunction wrap&lt;T&gt;(x: T): T { return x; }\n// strict: no any\n</code></pre>\n<h2>DOM</h2>\n<pre><code class=\"language-js\">el.addEventListener('click', handler); // не onclick=\nparent.addEventListener('click', e =&gt; {\n  if (e.target.matches('.btn')) { ... } // delegation\n});\n</code></pre>", "toc": [{"id": "cheatsheet-js-типы-и-сравнение", "label": "Типы и сравнение"}, {"id": "cheatsheet-js-массивы", "label": "Массивы"}, {"id": "cheatsheet-js-async", "label": "Async"}, {"id": "cheatsheet-js-event-loop-упрощённо-", "label": "Event Loop (упрощённо)"}, {"id": "cheatsheet-js-модули", "label": "Модули"}, {"id": "cheatsheet-js-typescript", "label": "TypeScript"}, {"id": "cheatsheet-js-dom", "label": "DOM"}], "text": "JavaScript &amp; TypeScript — шпаргалка После недель 5–10. Типы и сравнение Примитивы: string , number , boolean , null , undefined , bigint , symbol Всегда === , не == const по умолчанию; let если переприсвоение Массивы arr.map(x =&gt; x * 2) arr.filter(x =&gt; x.done) arr.find(x =&gt; x.id === id) arr.reduce((sum, x) =&gt; sum + x, 0) // slice — копия; splice — мутирует Async async function load() { const res = await fetch(url); if (!res.ok) throw new Error(res.status); return res.json(); } Event Loop (упрощённо) sync code → microtasks (Promise) → macrotasks (setTimeout) → render Модули // utils.js export function fmt(d) { ... } // app.js import { fmt } from './utils.js'; TypeScript interface User { id: number; name: string } type Status = 'active' | 'done'; function wrap&lt;T&gt;(x: T):…"}