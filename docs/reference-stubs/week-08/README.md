# Stub: week-08 Movie Catalog

Скопируйте в `learning-log/week-08/`.

```
week-08/
├── index.html
├── css/
├── js/
│   ├── main.js
│   ├── api.js       # fetch + error mapping
│   ├── ui.js        # render cards / states
│   └── storage.js   # favorites
├── config.example.js  # API key placeholder
└── README.md
```

`config.js` (из example) — в `.gitignore`. Для сдачи без ключа добавьте `mock/movies.json` и флаг `USE_MOCK=true`.
